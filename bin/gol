#!/usr/bin/env ruby
require 'gol'
require 'gol/fast_gol'

gun_array = Toys.add_arrays(Toys.empty(100, 40), Toys.glider_gun)

Window.new(Field.new(gun_array)).show
Window.new(World.new(gun_array)).show
