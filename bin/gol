#!/usr/bin/env ruby
require 'gol'
require 'gol/fast_gol'

Window.new(Field.new(Toys.empty(100, 100).add_array(Toys.glider_gun))).show
#Window.new(World.new(Toys.empty(100, 100).add_array(Toys.glider_gun))).show
